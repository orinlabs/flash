# Local dev: leave VITE_API_BASE_URL unset — Vite proxies `/api` to the API (see vite.config.ts).
# Production static build: set to your deployed API origin (no trailing slash).
# VITE_API_BASE_URL=https://api.flash.orinlabs.ai
