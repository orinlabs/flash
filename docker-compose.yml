services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: icp
      POSTGRES_PASSWORD: icp
      POSTGRES_DB: icp_prospector
    volumes:
      - icp_pg:/var/lib/postgresql/data

volumes:
  icp_pg:
